import {StyleSheet} from "react-native"

const styles = StyleSheet.create({
  text: {
    fontSize: 16,
    fontWeight: "bold",
    color: "#9E9E9E",
    marginBottom: 8
  }
})

export default styles
